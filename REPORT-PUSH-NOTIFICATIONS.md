# تقرير شامل — تفعيل الإشعارات الفورية (Push Notifications)

**تاريخ التقرير:** 7 سبتمبر 2026
**المشروع:** واجباتي (wajebaty.vercel.app)

---

## ١. الهدف

توصيل إشعارات فورية للأجهزة (هواتف الطلاب والمعلمين) عند حدوث أحداث مهمة: واجب جديد، رسالة جديدة، انتهاء موعد الواجب، إغلاق المحادثة. كان المطلوب أن يعمل النظام على **أجهزة iPhone (iOS)** بشكل خاص لأنها الأشدّ تشددًا في استقبال الإشعارات.

---

## ٢. مراحل التحقيق والتشخيص (بالترتيب)

### المرحلة ١ — التحقق من البنية التحتية في قاعدة البيانات
فحصنا حالة المكونات المطلوبة في Supabase:
1. **جدول الإعدادات `settings`** — يجب أن يحتوي على مفاتيح VAPID وبيانات سرية لخدمة الدفع.
2. **جدول الاشتراكات `push_subscriptions`** — تسجيل أجهزة الطلاب.
3. **المشغّل (Trigger) `notify_push_after_insert`** — يستدعي خدمة الويب تلقائيًا عند إضافة إشعار جديد.
4. **مجدول المهام (pg_cron)** — الوظائف الثلاث: `wajebaty-due-reminders` و`wajebaty-close-overdue` و`wajebaty-cleanup-closed-conversation-files`.
5. **امتداد `pg_net`** — يُرسل طلبات HTTPS من قاعدة البيانات إلى التطبيق (النتيجة: مثبّت، الإصدار 0.20.4).
6. **البث المباشر (Realtime)** — جدول `notifications` كان **غير مدرج** في قائمة النشر `supabase_realtime`، وهذا سبب عدم ظهور التنبيهات الفورية داخل التطبيق (تم الإصلاح).

**النتيجة:** كل المكونات تعمل بعد رفع البنية إلى الأوضاع الصحيحة.

---

### المرحلة ٢ — اكتشاف إعادة الكتابة الخاطئة لعنوان الويب هوك

المشكلة الأولى كانت أن التطبيق (سكربت `app/actions/push.ts`) كان **يعيد كتابة** حقل `push_webhook_url` في قاعدة البيانات تلقائيًا في كل مرة يُفتح فيها التطبيق، اعتمادًا على متغيرات البيئة. أي أن أي SQL نصوّبه كان يُمسح بعد أول فتح للصفحة.

أيضًا وُجد خطأ إملائي في ملف `.env`: العنوان المكتوب كان `https://wagebati.vercel.app` (بـ **e**) بينما الموقع الفعلي هو `wajebaty` (بـ **a**).

**الإصلاح في الكود:**
- جعلنا حقل `push_webhook_url` **نهائيًا يُخزَّن في قاعدة البيانات فقط** ولا يُعاد كتابته من أي متغير بيئة.
- صحّحنا العنوان في `.env` إلى `https://wajebaty.vercel.app`.
- ثبّتنا القيمة الصحيحة في قاعدة البيانات:
  `https://wajebaty.vercel.app/api/push/send`

**النتيجة:** سجل `net._http_response` أظهر **200** (ناجح) في وصول الطلبات إلى التطبيق بدل 404.

---

### المرحلة ٣ — اكتشاف سبب فشل التسليم (خطأ 403 من Apple)

بعد نجاح الاتصال بين قاعدة البيانات والتطبيق، كان الإشعار يصل إلى التطبيق لكن الهاتف لا يهتز. سجّل التطبيق النتيجة:
```
{"ok":true,"count":0,"failed":1,"details":
 [{"endpoint":"https://web.push.apple.com/...","status":403,
   "message":"Received unexpected response code"}]}
```

**التشخيص:** الجهاز مسجّل عبر **Apple Push Service** — والرقم `403` من Apple يعني حصريًا أن **مفتاح VAPID** (المفتاح العام للدفع) الذي سُجّل به الهاتف **لا يتطابق** مع المفتاح الحالي في قاعدة البيانات.

**السبب الجذري:** أثناء عمليات النشر القديمة، كان الكود يولّد مفاتيح VAPID جديدة في كل مرة (عند غيابها)، فيسجّل الهاتف بمفتاح، ويُغيَّر المفتاح بعد تحديث آخر، فيرفض Apple الإشعار من جهاز بمفتاح قديم. أيضًا الكود القديم لم يكن يفكّك الاشتراك القديم عند إعادة التفعيل.

---

## ٣. الإصلاحات النهائية المنفّذة

### أ) استقرار مفاتيح VAPID
- يُولَّد المفتاح الآن **مرة واحدة فقط** ويُخزَّن في `settings`، ولا يتجدد بعد التفعيل الناجح.

### ب) تتبّع المفتاح لكل جهاز
- أضفنا عمود `vapid_key` في جدول `push_subscriptions` ويُخزَّن مع كل اشتراك جديد المفتاح الذي أنشأه.
- عند فتح التطبيق، يقارن الكود المفتاح المخزّن مع المفتاح الحالي:
  - إذا تطابقا → الوضع "مفعلة".
  - إذا اختلفا → **يفك اشتراك الجهاز تلقائيًا ويُعيد تسجيله بالمفتاح الصحيح** دون أي إجراء من المستخدم.

### ج) حماية من جانب الخادم
- عند إرسال الدفع، يفحص الخادم مفتاح كل اشتراك ولا يرسل إلا للأجهزة بمفاتيح مطابقة، ويحذف القديمة تلقائيًا (يُمنع تكرار خطأ 403 مستقبلًا).

### د) عرض تفاصيل الأخطاء
- يُظهر ملف `app/api/push/send/route.ts` الآن تفاصيل الخطأ الدقيقة (النهاية + رمز الحالة) في استجابة HTTP، لتقييمها مباشرة من الجداول.

### هـ) إصلاحات مرتبطة (من جولات سابقة)
- إدراج جدول `notifications` في قائمة نشر البث المباشر (إظهار فوري داخل التطبيق).
- تفعيل `pg_net` (الإصدار 0.20.4).
- تركيب دالة `notify_push_after_insert` بطريقة لا تمنع الإدراج أبدًا حتى لو فشل الدفع.
- مضاعفة حد معدل الطلبات في `app/api/push/send/route.ts` إلى 500/دقيقة.

---

## ٤. الملفات المتأثرة

| الملف | الغرض |
|---|---|
| `app/actions/push.ts` | توليد/إبقاء مفاتيح VAPID، حفظ الاشتراك مع المفتاح، عدم المساس بعنوان الويب هوك |
| `app/api/push/send/route.ts` | إرسال الإشعارات، تصفية الأجهزة ذات المفاتيح القديمة، تفاصيل الأخطاء، حد المعدل |
| `components/push-enabler.tsx` | زر التفعيل/الإيقاف — إعادة اشتراك تلقائية عند اختلاف المفتاح |
| `scripts/live-fixes.sql` | إصلاحات شاملة لتشغيل قاعدة البيانات |
| `scripts/video-and-push.sql` | رفع سعة التخزين + أدوات تشخيص الدفع |
| `scripts/fix-apple-push.sql` | إضافة عمود `vapid_key` وتنظيف الاشتراكات القديمة (إصلاح 403) |
| `.env` | تصحيح اسم النطاق إلى `wajebaty.vercel.app` |

---

## ٥. خطوات التحقق لمرة أخيرة (بعد التقرير)

1. تشغيل سكربت `scripts/fix-apple-push.sql` في SQL Editor.
2. فتح التطبيق على جهاز الطالب (إخراج التطبيق من المهمة ثم فتحه مجددًا) ليُسجَّل الاشتراك الجديد تلقائيًا.
3. إرسال رسالة من حساب المعلم.
4. التحقق من نتيجة السجل:

```sql
select status_code, created from net._http_response order by created desc limit 5;
select left(content, 300) from net._http_response where status_code = 200 order by created desc limit 1;
```

النتيجة المتوقعة: `{"ok":true,"count":1,"failed":0}` واهتزاز هاتف الطالب.

---

## ٦. حالة النظام بعد الإصلاح

- ✅ الاتصال بين قاعدة البيانات والتطبيق: يعمل (رمز 200).
- ✅ ظهور الإشعار داخل التطبيق عند فتحه: يعمل (Realtime).
- ✅ تسجيل الجهاز بمفتاح صحيح: يعمل (تلقائي عند فتح التطبيق بالإصدار الجديد).
- 🕒 **خطوة أخيرة قيد التحقق** — وصول الإشعار إلى هاتف الطالب الفعلي بعد إعادة التسجيل بالمفتاح الصحيح.

---

*تم العمل بتكامل مستمر مع قاعدة بيانات Supabase وبيئة Vercel، مع التحقق من كل طبقة: قاعدة البيانات → التطبيق → خدمة الدفع (Apple) → الجهاز.*